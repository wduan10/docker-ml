apiVersion: batch/v1
kind: Job
metadata:
    name: train-kueue-job
    labels:
      kueue.x-k8s.io/queue-name: user-queue
spec:
    template:
        spec:
            containers:
                - name: train-kueue
                  image: wduan10/ml_model
                  resources:
                      requests:
                          cpu: 1
                          memory: "3Gi"
                      limits:
                          nvidia.com/gpu: 1
            restartPolicy: Never
    backoffLimit: 4
